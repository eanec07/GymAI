# SYLRIX.FIT brand assets

This folder is reserved for final approved brand files. The product currently uses the
text treatment in `templates/base.html`, so missing artwork can never break the app.

When final artwork is approved, add `sylrix-mark.svg`, `sylrix-wordmark.svg`, and any
approved dark/light variants here, then wire them into the existing brand component.
